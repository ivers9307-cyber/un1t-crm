// Pipeline tab — Phase 4 stub.

import { View, Text, ScrollView } from 'react-native'

export default function Pipeline() {
  return (
    <ScrollView className="flex-1 bg-un1t-black" contentContainerClassName="p-6">
      <Text className="text-2xl font-bold text-un1t-white mb-2">Pipeline</Text>
      <Text className="text-base text-un1t-light">
        Coming in Phase 4. Stage-by-stage swipeable view, contact detail,
        log call/note, move deal between stages.
      </Text>
    </ScrollView>
  )
}
