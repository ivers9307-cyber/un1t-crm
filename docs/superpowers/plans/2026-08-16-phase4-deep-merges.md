# Phase 4 — Deep Merges (Money completion + Marketing/Messages split) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans. Checkbox steps. Grounding: the 2026-08-16 phase-4/5 recon (all facts below verified there).

**Spec amendments this plan makes (grounded, documented for Richard's PR review):**
- **4A goes zero-churn.** Moving `/schedule/invoices` + `/schedule/expenses` under Money URLs would cost a NEW permission key (submitters hold no Money-union key; their only nav path is Team→Schedule tabs) or a split surface — purchased only for URL aesthetics, since the approvals rows, emails, and `?focus` params (dead today) work either way. Instead: Money gains two cross-hub TABS (Live-HR precedent — tab href outside the group, no strip on arrival) gated on the APPROVER keys, so reviewers reach the queues from Money; submitters keep Team→Schedule. No URL moves, no redirects, no new keys.
- **4B moves the campaign lifecycle wholesale.** Send + Sent (+ `[channel]/[id]` editors) + Segments + list-health are ONE campaign lifecycle with zero inbox coupling (proven: the inbox composers are WAInbox/IGInbox/TicketCompose/ContactComposer hitting conversation endpoints — never /communications/send). Templates EDITOR surface moves to Marketing; template DATA stays cross-cutting infrastructure (inbox, Mia, booking emails all read it — nothing about those reads changes).

---

### Task 1 (4A): Money review tabs

`(money)/layout.js` TABS += `{ id: 'contractor-invoices', label: 'Contractor invoices', href: '/schedule/invoices', perms: ['approvals_contractor_invoices'] }` and `{ id: 'expenses', label: 'Staff expenses', href: '/schedule/expenses', perms: ['approvals_fte_expenses'] }` (comment: cross-hub review tabs, submit surface stays Team-side, spec §3.1 amended — recon 2026-08-16). Money entry union += the two approver keys (nav-items TDD). /money index chain: after `orders`, add the two approver keys → their hrefs. Sidebar double-light check: `/schedule/*` is in TEAM's extraActivePaths — the matcher's longest-match gives `/schedule/invoices` to... Team's `/schedule` prefix. Money tab lights via HubTabs on those pages? Those pages are OUTSIDE (money) — no Money strip there; Team strip shows (they're in (team)) — this is the accepted cross-hub-tab UX (Live HR/fleet precedent). Tests + full suite. Commit.

### Task 2 (4B): the (marketing) absorption

1. Move from `communications/(hub)/` into `(marketing)/`: `send/`, `sent/` (whole tree incl. `[channel]/[id]`), `segments/`, `list-health/`, plus `communications/(editors)/templates/**` → `(marketing)/templates-editors/…`? NO — keep URLs UNCHANGED: route groups are invisible, so `(marketing)/send` serves `/send`?? WRONG — moving between groups CHANGES the URL prefix only if the literal segments change. `/communications/send`'s URL comes from the literal `communications` dir. To keep URLs, the files must stay under a `communications/` literal path. THE MECHANISM: create `src/app/communications/(marketing-era)/` — a SECOND route group INSIDE communications (sibling of `(hub)` and `(editors)`) holding send/sent/segments/list-health with its OWN layout (Marketing-flavoured chrome: MarketingCommsTabs or reuse HubTabs with tabs [Send, Sent, Segments, List health]), while `(hub)` keeps inbox/tickets + the index + a slimmed CommunicationsTabs (Inbox, Email inbox only). URLs never change; the CHROME and OWNERSHIP split. Templates editors stay in `(editors)` (already chrome-free) — their back-links keep working (comms-back-links.test contract untouched).
2. Marketing hub tabs += Send (`/communications/send`, perms email|whatsapp|sms), Sent, Segments (manager-role — HubTabs perms can't express roles: add a `roles` field to the tab filter, small extension mirroring settings-tree's vocabulary), List health; Marketing entry union += `sms` (nav TDD; sms had NO conversational surface — Marketing-only key now, comment it). Messages entry union DROPS none (email_inbox/whatsapp stay; `email`+`sms` stay for one-off sends? — the Messages gate `communications/layout.js` keeps all four keys since /communications/send still lives under it; document).
3. The hub index page (`(hub)/page.js`): 4 of 6 cards emigrate conceptually — keep the index as the Messages landing (inbox/tickets cards + cross-links to Marketing's surfaces, cards relabelled); `email-hub-stats` consumers checked.
4. CommunicationsTabs slims to 2 badged tabs (keep the scroller machinery — harmless). Palette `/communications/send` entries: gates unchanged (URLs unchanged). Redirect second-hop aliases: UNCHANGED (URLs unchanged — the recon's item 5 concern evaporates under this mechanism).
5. Tests: comms-back-links contract must stay green; nav/palette suites; full suite + build (route list unchanged). Commits per move batch.

### Task 3: Finalize + PR + final review + merge

CHANGELOG (`DEEP.4`: the two spec amendments with their grounding; the zero-URL-churn mechanism for 4B; sms's new Marketing-only status) + PR + fable review (seams: the (hub)/(marketing-era)/(editors) triple-group coexistence, gate story per persona, the Segments role-tab extension) + merge per standing auth.
