'use client'

import { Loading } from '@/components/ui'

// RCOV.P2 — placeholder; replaced by the real runs/health panel in the
// next task. Keeps AccountingTabs buildable meanwhile.
export default function RunsHealthPanel() {
  return <Loading />
}
